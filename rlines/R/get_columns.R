#' Tabulate the unique values of a given column
#' 
#' @param data: The data frame that will be analyzed
#' @return A list of the columns of the data frame

get_columns<-function(data){
  return(names(data))
}